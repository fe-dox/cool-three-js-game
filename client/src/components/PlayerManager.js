export default class PlayerManager {
    constructor(player, animation, ID) {
        this.player = player;
        this.animation = animation;
        this.ID = ID;
        this.HP = 100;
        console.log(`player${this.playerID}`)
        // this.playerDiv = document.getElementById(`player${this.ID}`);
        // this.playerDiv.style.display = "flex";
        // this.hpSpan.document.getElementById(`player${this.playerID}HPSpan`);
    }


    addHP() {

    }

    removeHP() {

    }
}